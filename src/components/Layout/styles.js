export default theme => ({
  layout: {
    margin: theme.dimen.gapLarge,
    padding: theme.dimen.gapSmall,
    border: theme.border.appBorder,
    borderRadius: theme.shape.borderRadius,
    minWidth: 600,
  },
});
